# Set to "false" to disable Keycloak login entirely (local/dev convenience
# only). Only takes effect when served from localhost/127.0.0.1/::1. On its
# own this only skips the frontend login screen -- pair with the backend's
# AUTH_DISABLED=true (root .env.example) for API calls to actually work
# without Keycloak; see the root README's Quickstart section.
VITE_KEYCLOAK_ENABLED=true

VITE_KEYCLOAK_URL=https://your-keycloak-host
VITE_KEYCLOAK_REALM=your-realm
VITE_KEYCLOAK_CLIENT_ID=your-client-id

# Realm role names granting admin/editor (upload) access. Defaults to
# ts-icon-admin/ts-icon-editor if unset -- override to match whatever roles
# actually exist in your realm.
VITE_KEYCLOAK_ADMIN_ROLE=ts-icon-admin
VITE_KEYCLOAK_EDITOR_ROLE=ts-icon-editor

# Public (unauthenticated, read-only) image API
VITE_PUBLIC_API_URL=http://localhost:3000
# Admin/local (authenticated) API
VITE_ADMIN_API_URL=http://localhost:3001
